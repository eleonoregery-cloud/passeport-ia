{
  "name": "passeport-ia",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix apps/simulator/client && npm install --prefix apps/simulator/server && npm install --prefix apps/backoffice",
    "dev:simulator": "npm run dev --prefix apps/simulator",
    "dev:backoffice": "npm run dev --prefix apps/backoffice",
    "build:simulator": "npm run build --prefix apps/simulator",
    "build:backoffice": "npm run build --prefix apps/backoffice"
  }
}
